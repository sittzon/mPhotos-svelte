export const config =
{
    // GENERATED_THUMBNAILS: '', // Leave empty or undefined if using docker
    // ORIGINAL_PHOTOS: '', // Leave empty or undefined if using docker
    THUMBNAIL_SIZE: 300,
    MEDIUM_SIZE: 1200,
    METADATA_FILE: 'metadata.json',
    ERRORS_FILE: 'errors.log'
};