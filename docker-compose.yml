services:
  client:
    image: mphotos-svelte
    container_name: mPhotos-svelte
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 5174:5174
    volumes:
      # Map originals folder, and area for thumbnails to be created
      - :/originals
      - :/thumbs